import { ProveraVracanjaDirective } from './provera-vracanja.directive';

describe('ProveraVracanjaDirective', () => {
  it('should create an instance', () => {
    const directive = new ProveraVracanjaDirective();
    expect(directive).toBeTruthy();
  });
});
